#include<stdio.h>
int main()
{
     int age,n;
     printf("Enter your age :");
     scanf("%d",&age);
     n = 18 - age;
     if(age >= 18)
     {
          printf("You can vote ");
     }
     else
     {
          printf("You have to wait %d more years for voting",n);
     }
}
